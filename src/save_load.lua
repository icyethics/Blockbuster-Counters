-- local o_cardsave = Card.save
-- function Card:save()
--     local cardTable = o_cardsave(self)
--     cardTable["counter"] = self.counter
--     cardTable["counterconfig"] = self.counterconfig
-- end

-- local o_cardload = Card.load
-- function Card:load(cardTable, other_card)
--     o_cardload(self, cardTable, other_card)
    

-- end